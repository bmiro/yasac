procedure prova(a: in character; b: in integer; c: in out integer; d: out character) is
	type rang is new integer range 1..10;
	type temp is array(rang) of integer;
	t: temp;
	e: integer;
	r: rang;
begin

	if c < b then
		c := c + 1;
	end if;

end prova;

		
