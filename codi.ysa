procedure prova(a: in character; b: in integer; c: in out integer; d: out character) is
	type rang is new integer range 1..20;
	type temp is array(rang, rang) of integer;
	t: temp;
	e: integer;
	r: rang;
	procedure prog(b: in boolean; w: out integer) is
		a : integer;
	begin
	 a := 1;
	end prog;
	bol : boolean;
begin

	if c < b then
		c := c + 1;
	end if;
	e := t(r, r); 
	prog(bol, c);
end prova;

		
