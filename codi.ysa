procedure prova(a:in integer) is
--	type x is array(1..max) of character;
	y : integer;
	type s is 
		record
			s : character;
			t : character;
		end record;
begin
	i(0) := 'a';	
	put("holaaa");
	p(contador, a(2), inc, 2);
	if a(3) = b(3) and c(5) /= 3 then
		b; 
		while 1 = 1 loop
			s := s + s(x);
		end loop;
	else 
		p := 3;
		if a = 2 then
			id(3) := t + 4;
		end if;
	end if;
	a := 4;
	id := prova(2);
	--comentari " "adfadsfds
end prova;


		
